import { NumberAlias } from "@svgdotjs/svg.js";

export interface dataType {
  width: NumberAlias;
  height: NumberAlias;
  x: NumberAlias;
  y: NumberAlias;
  point?: any;
  transform?: any;
  fill: string;
}
